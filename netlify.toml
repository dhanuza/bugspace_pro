# Netlify SPA configuration for Bugspace Pro.
# All routes are served from index.html so React Router handles client-side navigation.

[build]
  command   = "npm run build"
  publish   = "dist"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
